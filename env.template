# ========================================
# CONFIGURAÇÃO: DESENVOLVIMENTO
# ========================================
# Copie para: .env.development
# Use quando: Desenvolvimento local (npm run dev)

NODE_ENV=development
PORT=3000

# MongoDB Atlas - Database de DESENVOLVIMENTO
# IMPORTANTE: Use 'resolveai-dev' como nome do database
# Substitua: SEU_USUARIO, SUA_SENHA, SEU_CLUSTER
MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@SEU_CLUSTER.mongodb.net/resolveai-dev?retryWrites=true&w=majority

# CORS - Permitir localhost
CORS_ORIGINS=http://localhost:8081,http://localhost:19000,http://localhost:19006

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Autenticação do Dashboard
JWT_SECRET=altere-esta-chave-no-railway
JWT_EXPIRES_IN=8h
ADMIN_BOOTSTRAP_TOKEN=defina-um-token-de-seguranca

# Processamento de Imagem (LGPD Compliance)
# Ativar processamento automático de imagens para aplicar blur em rostos e placas
ENABLE_IMAGE_ANONYMIZATION=false

# Cloudinary (para upload de imagens processadas)
CLOUDINARY_CLOUD_NAME=durusveju
CLOUDINARY_API_KEY=855151415331897
CLOUDINARY_API_SECRET=zoFXPyejO8GHUz2tqU7KMvEh3yM

# Google Cloud Vision (para detecção de rostos e placas)
# Opção 1: Caminho para arquivo JSON de credenciais
GOOGLE_APPLICATION_CREDENTIALS=./config/google-vision-credentials.json

# Opção 2: Credenciais como JSON string (para Railway/produção)
# GOOGLE_CLOUD_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# ========================================
# SMTP - Envio de E-mails (Gmail)
# ========================================
# Para usar Gmail SMTP, você precisa:
# 1. Ativar verificação em 2 etapas na conta Google
# 2. Criar uma "Senha de App" em: https://myaccount.google.com/apppasswords
# 3. Usar essa senha de app no SMTP_PASS (não a senha normal do Gmail)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=holkzdevops@gmail.com
SMTP_PASS=fmqrkoeqdfqcayvz
SMTP_FROM_NAME=ResolveAi-Mobile


# ========================================
# CONFIGURAÇÃO: PRODUÇÃO
# ========================================
# Configure no Railway (variáveis de ambiente)
# NÃO crie arquivo .env.production localmente

# NODE_ENV=production
# PORT=3000

# MongoDB Atlas - Database de PRODUÇÃO
# IMPORTANTE: Use 'resolveai-prod' como nome do database
# MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@SEU_CLUSTER.mongodb.net/resolveai-prod?retryWrites=true&w=majority

# CORS - URLs do app em produção
# CORS_ORIGINS=https://seu-app.railway.app

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Autenticação do Dashboard
# JWT_SECRET=altere-esta-chave-no-railway
# JWT_EXPIRES_IN=8h
# ADMIN_BOOTSTRAP_TOKEN=defina-um-token-de-seguranca

# ========================================
# NOTA IMPORTANTE
# ========================================
# A ÚNICA DIFERENÇA entre dev e prod é o nome do database:
#   - Development: /resolveai-dev
#   - Production:  /resolveai-prod
#
# Ambos usam o MESMO CLUSTER do MongoDB Atlas!
# Isso mantém os dados separados mas usa apenas 1 free tier.

