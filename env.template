# ========================================
# CONFIGURAÇÃO: DESENVOLVIMENTO
# ========================================
# Copie para: .env.development
# Use quando: Desenvolvimento local (npm run dev)

NODE_ENV=development
PORT=3000

# MongoDB Atlas - Database de DESENVOLVIMENTO
# IMPORTANTE: Use 'resolveai-dev' como nome do database
# Substitua: SEU_USUARIO, SUA_SENHA, SEU_CLUSTER
MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@SEU_CLUSTER.mongodb.net/resolveai-dev?retryWrites=true&w=majority

# CORS - Permitir localhost
CORS_ORIGINS=http://localhost:8081,http://localhost:19000,http://localhost:19006

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100


# ========================================
# CONFIGURAÇÃO: PRODUÇÃO
# ========================================
# Configure no Railway (variáveis de ambiente)
# NÃO crie arquivo .env.production localmente

# NODE_ENV=production
# PORT=3000

# MongoDB Atlas - Database de PRODUÇÃO
# IMPORTANTE: Use 'resolveai-prod' como nome do database
# MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@SEU_CLUSTER.mongodb.net/resolveai-prod?retryWrites=true&w=majority

# CORS - URLs do app em produção
# CORS_ORIGINS=https://seu-app.railway.app

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# NOTA IMPORTANTE
# ========================================
# A ÚNICA DIFERENÇA entre dev e prod é o nome do database:
#   - Development: /resolveai-dev
#   - Production:  /resolveai-prod
#
# Ambos usam o MESMO CLUSTER do MongoDB Atlas!
# Isso mantém os dados separados mas usa apenas 1 free tier.

